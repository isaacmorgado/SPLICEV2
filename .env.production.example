# ===========================================
# PRODUCTION ENVIRONMENT VARIABLES
# ===========================================
# Copy this file to .env.production and fill in your actual values
# NEVER commit .env.production to version control

# ===========================================
# Plugin Configuration (Vite Build)
# ===========================================

# Backend API URL - Your deployed Vercel app URL
# Example: https://splice-api.vercel.app/api
VITE_BACKEND_URL=https://your-vercel-app.vercel.app/api

# ===========================================
# Database Configuration
# ===========================================

# Neon Postgres Connection String
# Get from: https://console.neon.tech
# Format: postgres://user:password@host.neon.tech/dbname?sslmode=require
DATABASE_URL=postgres://user:password@host.neon.tech/splice?sslmode=require

# ===========================================
# Authentication
# ===========================================

# JWT Secret - MUST be a strong random string (min 32 characters)
# Generate with: openssl rand -base64 32
JWT_SECRET=your_production_jwt_secret_minimum_32_characters_required

# ===========================================
# Stripe Configuration
# ===========================================

# Stripe Secret Key (LIVE mode - starts with sk_live_)
# Get from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_live_your_live_stripe_secret_key

# Stripe Webhook Secret (for production webhook endpoint)
# Get from: https://dashboard.stripe.com/webhooks
# IMPORTANT: Set webhook URL to https://your-app.vercel.app/api/stripe/webhook
STRIPE_WEBHOOK_SECRET=whsec_your_production_webhook_secret

# Stripe Price IDs (LIVE mode - starts with price_)
# Create products in Stripe Dashboard: https://dashboard.stripe.com/products
STRIPE_PRO_PRICE_ID=price_your_pro_tier_price_id
STRIPE_STUDIO_PRICE_ID=price_your_studio_tier_price_id

# ===========================================
# AI Services Configuration
# ===========================================

# OpenAI API Key
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your_production_openai_api_key

# OpenAI API Endpoint (default or custom proxy)
AI_SERVICE_URL=https://api.openai.com/v1

# ElevenLabs API Key (Voice Isolation)
# Get from: https://elevenlabs.io
ELEVENLABS_API_KEY=your_production_elevenlabs_api_key

# Gemini API Key (Alternative LLM for take analysis)
# Get from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_production_gemini_api_key

# ===========================================
# Application Settings
# ===========================================

# Node Environment
NODE_ENV=production

# Debug Mode - Set to false in production
DEBUG_MODE=false

# ===========================================
# DEPLOYMENT NOTES
# ===========================================
#
# 1. Vercel Environment Variables:
#    - Add all variables above (except VITE_* ones) to Vercel project settings
#    - Go to: https://vercel.com/[your-org]/[your-project]/settings/environment-variables
#
# 2. Build-time Variables (VITE_*):
#    - Add VITE_BACKEND_URL to Vercel environment variables
#    - It will be embedded during build time
#
# 3. Stripe Webhook Setup:
#    - Create webhook endpoint: https://dashboard.stripe.com/webhooks
#    - URL: https://your-app.vercel.app/api/stripe/webhook
#    - Events to listen: customer.subscription.created, customer.subscription.updated,
#      customer.subscription.deleted, invoice.payment_succeeded, invoice.payment_failed
#    - Copy webhook secret to STRIPE_WEBHOOK_SECRET
#
# 4. Database Setup:
#    - Run db/schema.sql in your Neon production database
#    - Use scripts/db-migrate.sh for migrations
#
# 5. Security Checklist:
#    - Ensure JWT_SECRET is strong and unique (32+ characters)
#    - Use LIVE Stripe keys (sk_live_*, price_*, whsec_*)
#    - Enable SSL for database connection (sslmode=require)
#    - Review CORS settings in vercel.json
#    - Test webhook endpoints with Stripe CLI
